Скрипт : 
1. Нужно создать отдельную функцию для присваивания простых значений в .passageInformation
Например : переписать с

let leftGateway = floorPlan[cordRoom].passageInformation.left;
if (cordRoom % 10 == 0 &&
    leftGateway == true) {
    floorPlan[cordRoom].passageInformation.left = false;
};
 
На

const editArgum = (obj, arg, parm) => {
    Object.defineProperty(obj, arg, parm)
};

let pasInfo = floorPlan[cordRoom].passageInformation;
if (cordRoom % 10 == 0 &&
    leftGateway == true) {
    editArgum(pasInfo, 'nameArgument', {value: value})
};

2. Переписать функцию genRing, для того что бы она генерировал не кольца 3*3, а замкнутые кривые заданой длины. При этом длинна кривой будет определяться количеством комнат, входящих в неё.

3. Исправить баг гинерации сида. когда при значении 012 сидНам образуеться такой же как и при 5.2 
   сделать функцию генерации сидНам как перевод цифры из одной системы счисления в другую 
   функция должна примерно выглядеть вот так "seed[i] * abc.length ^ (seed.length-(1+i))" где i от 0 до seed.length
   Сделать случаную генерацию сида если он не введён пользователем, ограничить случайный сид 8ю символами, а сид пользователя 15тью символами

4. Вынести строку abc как отдельный словарь, в отдельном файле. Туда же можно передать from и to.

5. Сделать Раздачу комнат. Тоесть что бы уже были не просто коридоры, повороты и т.д. А уже полноценно с теплицами и т.д. (Перед этим сохранвив алгоритм с уже готовыми усовершенствиями, в папку Конрольная версия)

6. Попробывать сделать Объявление локальных переменных через функцию, ДЛЯ оптимизации кода (то есть что бы не обявлять каждый раз верхнюю нижнюю комнату и т.д. вызывать функцию, и потом пользоваться доступными переменными)



HTML : 
1. Сделать обозначения особых комнат на карте в веб станице. В виде цветного текста который находиться поверх картинок. 
2. Вынести в отдельную папку все картинки. И добавить favicon

CSS :
1. Отцентровать по горизонтале и вертикале текст который обозначает особые комнаты